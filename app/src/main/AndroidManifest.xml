<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiniMoodle"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".activites.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activites.RegisterActivity" />
        <activity android:name=".activites.DashboardActivity" />
        <activity android:name=".activites.CoursListActivity" />
        <activity android:name=".activites.CoursDetailActivity" />
        <activity android:name=".activites.TravailDetailActivity" />
        <activity android:name=".activites.QuizListActivity" />
        <activity android:name=".activites.QuizActivity" />
        <activity android:name=".activites.QuizResultatActivity" />
        <activity android:name=".activites.ProfilActivity" />
        <activity android:name=".activites.TravailInfoActivity" />

    </application>

</manifest>
